.fields_group
  = f.label t(:'site.user.new.social_activities.activity_type')
  = f.text_field :activity_type, :size => 20

  = f.label  t(:'site.user.new.social_activities.duties')
  = f.text_field :duties, :size => 20

  = f.label  t(:'site.user.new.social_activities.position')
  = f.text_field :position, :size => 20

  = link_to_remove_fields t(:'site.user.new.actions.remove'), f


